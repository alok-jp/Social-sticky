version: '3.8'
services:
  mongo:
    image: mongo:6
    container_name: ssn_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: ./backend
    container_name: ssn_backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      - mongo
    environment:
      MONGODB_URI: mongodb://mongo:27017/social-sticky-notes

  frontend:
    build: ./frontend
    container_name: ssn_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  mongo_data:
